# Self-hosted runner config for postcard (Rails). STUB — confirm REPO_NAME and the
# labels postcard's ci.yml targets, then add a PAT in apps/postcard.secret.env.
REPO_OWNER=d3vkit
REPO_NAME=postcard        # TODO: confirm the actual repo name
STACK=rails
RUNNER_LABELS=self-hosted,linux,postcard
RUNNER_NAME=postcard-mac-runner
