# Copy to apps/<app>.secret.env (gitignored) and set the PAT for that app:
#   cp apps/secret.env.example apps/kyra.secret.env
#
# Fine-grained GitHub PAT scoped to the app's repo, with
#   Repository permissions → Administration: Read and write
# (needed to mint runner registration tokens). One PAT per app.
GITHUB_PAT=
